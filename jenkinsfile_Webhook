pipeline {
   agent any
    stages {
         stage('pre -build') {
            steps {
                sh 'echo Pre-build'
                sh 'python3 --version'
            }
        }
        stage('build') {
            steps {
                
                echo 'build in progress'
                sh 'python3  Webhook/main.py'
            }
        }
    }
    stages {
         stage('test-build') {
            steps {
                sh 'echo test-build'
                sh 'python3 --version'
            }
        }
        stage('week6') {
            steps {
                
                echo 'week6 dev tool'
                sh 'python3  Webhook/main.py'
            }
        }
    }
}